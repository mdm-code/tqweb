// Package interpreter provides the Interpreter struct that implements the
// ast.Visitor interface allowing it to walk and interpret the tq AST into
// sequence of filtering functions processing TOML input data as specified in
// the query.
package interpreter
